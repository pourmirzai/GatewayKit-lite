(function($) { 'use strict'; window.GatewayKit = window.GatewayKit || {}; window.GatewayKit.Admin = window.GatewayKit.Admin || {}; var AdminTransactions = { escapeHtml: function(text) { if (text == null) { return ''; } return String(text) .replace(/&/g, '&amp;') .replace(/</g, '&lt;') .replace(/>/g, '&gt;') .replace(/"/g, '&quot;') .replace(/'/g, '&#039;'); }, init: function() { this.bindEvents(); }, bindEvents: function() { var self = this; // Filter and search buttons: allow native submit $(document).on('click', '#gatewaykit-filter-btn, #gatewaykit-search-btn', function() { // Let native form submit proceed }); // Confirm bulk delete on WP_List_Table bulk form and prevent empty selection $(document).on('submit', 'form', function(e) { self.handleBulkDelete(e); }); // Transaction details redirect $('.gatewaykit-transaction-details').on('click', function(e) { e.preventDefault(); var transactionId = $(this).data('transaction-id'); window.location.href = gatewaykit_admin_vars.transaction_url + transactionId; }); // Form data popup $('.gatewaykit-view-form-data').on('click', function(e) { e.preventDefault(); var transactionId = $(this).data('transaction-id'); self.showFormDataModal(transactionId); }); // Notes popup $(document).on('click', '.gatewaykit-view-notes', function(e) { e.preventDefault(); var transactionId = $(this).data('transaction-id'); self.showNotesModal(transactionId); }); // Error details popup $('.gatewaykit-error-info').on('click', function(e) { e.preventDefault(); var transactionId = $(this).closest('tr').find('.gatewaykit-view-form-data').data('transaction-id'); if (transactionId) { self.showErrorDetailsModal(transactionId); } }); // Close modal $(document).on('click', '.gatewaykit-modal-close, .gatewaykit-modal-overlay', function() { $('.gatewaykit-modal').hide(); }); // Copy error details to clipboard $(document).on('click', '.gatewaykit-copy-button', function(e) { e.preventDefault(); self.handleCopyToClipboard($(this)); }); // Save transaction note $(document).on('click', '.gatewaykit-save-note', function(e) { e.preventDefault(); var btn = $(this); var transactionId = btn.data('transaction-id'); var note = $('#gatewaykit-new-note').val(); if (!note || !note.trim()) { return; } btn.prop('disabled', true).text(gatewaykit_admin_vars.saving); $.ajax({ url: gatewaykit_ajax.ajax_url, type: 'POST', data: { action: 'gatewaykit_add_transaction_note', transaction_id: transactionId, note: note, nonce: gatewaykit_ajax.nonce }, success: function(response) { if (response.success) { self.renderNotes(response.data.notes, transactionId); } else { alert(response.data.message || gatewaykit_admin_vars.error); btn.prop('disabled', false).text(gatewaykit_admin_vars.save_note); } }, error: function() { alert(gatewaykit_admin_vars.ajax_error_occurred); btn.prop('disabled', false).text(gatewaykit_admin_vars.save_note); } }); }); // Copy shortcode to clipboard $(document).on('click', '.gatewaykit-copy-shortcode', function(e) { e.preventDefault(); var button = $(this); var textToCopy = button.data('copy-text'); if (navigator.clipboard && window.isSecureContext) { navigator.clipboard.writeText(textToCopy).then(function() { self.showCopyShortcodeFeedback(button); }); } else { var textArea = document.createElement('textarea'); textArea.value = textToCopy; textArea.style.position = 'fixed'; textArea.style.left = '-999999px'; textArea.style.top = '-999999px'; document.body.appendChild(textArea); textArea.focus(); textArea.select(); try { document.execCommand('copy'); self.showCopyShortcodeFeedback(button); } catch (err) { // Copy fallback failed } document.body.removeChild(textArea); } }); }, handleBulkDelete: function(e) { var $form = $(e.currentTarget); if ($form.find('table.wp-list-table').length === 0) { return; } var action1 = $form.find('select[name="action"]').val() || ''; var action2 = $form.find('select[name="action2"]').val() || ''; var actionVal = action1 && action1 !== '-1' ? action1 : (action2 && action2 !== '-1' ? action2 : ''); if (actionVal === 'delete') { var selectedCount = $form.find('input[name="transaction_ids[]"]:checked').length; if (selectedCount === 0) { alert(gatewaykit_admin_vars.validation_error); e.preventDefault(); return false; } if (!window.confirm(gatewaykit_admin_vars.confirm_delete)) { e.preventDefault(); return false; } } }, handleCopyToClipboard: function(button) { var self = this; var textToCopy = button.data('copy-text') || button.closest('.gatewaykit-error-details').text(); if (navigator.clipboard && window.isSecureContext) { navigator.clipboard.writeText(textToCopy).then(function() { self.showCopyFeedback(button); }); } else { var textArea = document.createElement('textarea'); textArea.value = textToCopy; textArea.style.position = 'fixed'; textArea.style.left = '-999999px'; textArea.style.top = '-999999px'; document.body.appendChild(textArea); textArea.focus(); textArea.select(); try { document.execCommand('copy'); self.showCopyFeedback(button); } catch (err) { // Fallback failed } document.body.removeChild(textArea); } }, showCopyFeedback: function(button) { var originalText = button.text(); button.text(gatewaykit_admin_vars ? gatewaykit_admin_vars.copied : 'Copied!').addClass('copied'); setTimeout(function() { button.text(originalText).removeClass('copied'); }, 2000); }, showCopyShortcodeFeedback: function(button) { var icon = button.find('.dashicons'); var text = button.find('.gatewaykit-copy-text'); var originalIconClass = 'dashicons-clipboard'; var originalText = text.text(); button.addClass('gatewaykit-copied'); icon.removeClass(originalIconClass).addClass('dashicons-yes-alt'); text.text(gatewaykit_admin_vars.copied || 'Copied!'); setTimeout(function() { button.removeClass('gatewaykit-copied'); icon.removeClass('dashicons-yes-alt').addClass(originalIconClass); text.text(originalText); }, 2000); }, formatCurrency: function(amount, currency) { currency = currency || gatewaykit_admin_vars.default_currency || 'USD'; return this.number_format(amount, 2) + ' ' + currency.toUpperCase(); }, number_format: function(number, decimals, decPoint, thousandsSep) { number = (number + '').replace(/[^0-9+\-Ee.]/g, ''); var n = !isFinite(+number) ? 0 : +number; var prec = !isFinite(+decimals) ? 0 : Math.abs(decimals); var sep = (typeof thousandsSep === 'undefined') ? ',' : thousandsSep; var dec = (typeof decPoint === 'undefined') ? '.' : decPoint; var s = ''; var toFixedFix = function(n, prec) { var k = Math.pow(10, prec); return '' + (Math.round(n * k) / k).toFixed(prec); }; s = (prec ? toFixedFix(n, prec) : '' + Math.round(n)).split('.'); if (s[0].length > 3) { s[0] = s[0].replace(/\B(?=(?:\d{3})+(?!\d))/g, sep); } if ((s[1] || '').length < prec) { s[1] = s[1] || ''; s[1] += new Array(prec - s[1].length + 1).join('0'); } return s.join(dec); }, showFormDataModal: function(transactionId) { var self = this; var modal = $('#gatewaykit-form-data-modal'); var content = $('#gatewaykit-form-data-content'); content.html('<p>' + self.escapeHtml(gatewaykit_admin_vars.loading) + '</p>'); modal.show(); $.ajax({ url: gatewaykit_ajax.ajax_url, type: 'POST', data: { action: 'gatewaykit_get_form_data', transaction_id: transactionId, nonce: gatewaykit_ajax.nonce }, success: function(response) { if (response.success) { var data = response.data; var html = '<div class="gatewaykit-form-data">'; html += '<div class="gatewaykit-transaction-info">'; html += '<h3>' + self.escapeHtml(gatewaykit_admin_vars.transaction_details) + '</h3>'; html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars.id) + ':</strong> ' + self.escapeHtml(data.transaction_id) + '</p>'; html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars.amount) + ':</strong> ' + self.formatCurrency(data.amount, data.currency) + '</p>'; html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars.status) + ':</strong> ' + self.escapeHtml(data.status) + '</p>'; html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars.date) + ':</strong> ' + self.escapeHtml(data.created_at) + '</p>'; if (data.user_id) { html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars.user_id) + ':</strong> ' + self.escapeHtml(data.user_id) + '</p>'; } html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars.description) + ':</strong> ' + self.escapeHtml(data.description || '-') + '</p>'; html += '</div>'; // Form data if (data.form_data && Object.keys(data.form_data).length > 0) { html += '<div class="gatewaykit-form-fields">'; html += '<h3>' + self.escapeHtml(gatewaykit_admin_vars.form_data) + '</h3>'; html += '<table class="wp-list-table widefat fixed striped">'; html += '<thead><tr><th>' + self.escapeHtml(gatewaykit_admin_vars.field) + '</th><th>' + self.escapeHtml(gatewaykit_admin_vars.value) + '</th></tr></thead><tbody>'; $.each(data.form_data, function(name, field) { var fieldName = name; var fieldValue = '-'; if (typeof field === 'object' && field !== null) { if (field.title) { fieldName = field.title; } else if (field.id) { fieldName = field.id; } if (field.value !== undefined) { fieldValue = field.value; } else if (field.raw_value !== undefined) { fieldValue = field.raw_value; } else { fieldValue = JSON.stringify(field); } } else { fieldValue = field; } html += '<tr>'; html += '<td><strong>' + self.escapeHtml(fieldName) + '</strong></td>'; html += '<td>' + self.escapeHtml(fieldValue) + '</td>'; html += '</tr>'; }); html += '</tbody></table>'; html += '</div>'; } else { html += '<p>' + self.escapeHtml(gatewaykit_admin_vars.no_form_data) + '</p>'; } // User data if (data.user_data && Object.keys(data.user_data).length > 0) { html += '<div class="gatewaykit-user-data">'; html += '<h3>' + self.escapeHtml(gatewaykit_admin_vars.user_data) + '</h3>'; html += '<table class="wp-list-table widefat fixed striped">'; html += '<thead><tr><th>' + self.escapeHtml(gatewaykit_admin_vars.field) + '</th><th>' + self.escapeHtml(gatewaykit_admin_vars.value) + '</th></tr></thead><tbody>'; $.each(data.user_data, function(key, value) { html += '<tr>'; html += '<td><strong>' + self.escapeHtml(key) + '</strong></td>'; html += '<td>' + self.escapeHtml(value || '-') + '</td>'; html += '</tr>'; }); html += '</tbody></table>'; html += '</div>'; } html += '</div>'; content.html(html); } else { content.html('<p>' + self.escapeHtml(gatewaykit_admin_vars.error) + ': ' + self.escapeHtml(response.data || gatewaykit_admin_vars.unknown_error) + '</p>'); } }, error: function() { content.html('<p>' + self.escapeHtml(gatewaykit_admin_vars.ajax_error_occurred) + '</p>'); } }); }, showNotesModal: function(transactionId) { var self = this; var modal = $('#gatewaykit-notes-modal'); $('#gatewaykit-notes-content').html('<p>' + self.escapeHtml(gatewaykit_admin_vars.loading) + '</p>'); modal.show(); this.loadTransactionNotes(transactionId); }, loadTransactionNotes: function(transactionId) { var self = this; var container = $('#gatewaykit-notes-content'); if (!container.length) return; container.html('<p>' + self.escapeHtml(gatewaykit_admin_vars.loading) + '</p>'); $.ajax({ url: gatewaykit_ajax.ajax_url, type: 'POST', data: { action: 'gatewaykit_get_transaction_notes', transaction_id: transactionId, nonce: gatewaykit_ajax.nonce }, success: function(response) { if (response.success) { self.renderNotes(response.data.notes, transactionId); } else { container.html('<p>' + self.escapeHtml(gatewaykit_admin_vars.error) + '</p>'); } }, error: function() { container.html('<p>' + self.escapeHtml(gatewaykit_admin_vars.ajax_error_occurred) + '</p>'); } }); }, renderNotes: function(notes, transactionId) { var self = this; var container = $('#gatewaykit-notes-content'); var html = ''; html += '<h3>' + self.escapeHtml(gatewaykit_admin_vars.notes) + '</h3>'; if (notes && notes.length > 0) { html += '<div class="gatewaykit-notes-list">'; for (var i = 0; i < notes.length; i++) { var note = notes[i]; html += '<div class="gatewaykit-note-item">'; html += '<div class="gatewaykit-note-meta">'; html += '<strong>' + self.escapeHtml(note.author_name || gatewaykit_admin_vars.unknown_user) + '</strong>'; html += ' <span class="gatewaykit-note-date">' + self.escapeHtml(note.created_at) + '</span>'; html += '</div>'; html += '<div class="gatewaykit-note-text">' + self.escapeHtml(note.note) + '</div>'; html += '</div>'; } html += '</div>'; } else { html += '<p>' + self.escapeHtml(gatewaykit_admin_vars.no_notes) + '</p>'; } // Add note form html += '<div class="gatewaykit-add-note">'; html += '<textarea id="gatewaykit-new-note" placeholder="' + self.escapeHtml(gatewaykit_admin_vars.add_note_placeholder) + '" rows="3"></textarea>'; html += '<button type="button" class="button button-primary gatewaykit-save-note" data-transaction-id="' + transactionId + '">' + self.escapeHtml(gatewaykit_admin_vars.save_note) + '</button>'; html += '</div>'; container.html(html); }, showErrorDetailsModal: function(transactionId) { var self = this; if (!$('#gatewaykit-error-details-modal').length) { var modalHtml = '<div id="gatewaykit-error-details-modal" class="gatewaykit-modal gatewaykit-error-modal" style="display: none;">' + '<div class="gatewaykit-modal-overlay"></div>' + '<div class="gatewaykit-modal-content">' + '<div class="gatewaykit-modal-header">' + '<h2>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.error_details : 'Error Details') + '</h2>' + '<button type="button" class="gatewaykit-modal-close">&times;</button>' + '</div>' + '<div class="gatewaykit-modal-body">' + '<div id="gatewaykit-error-details-content">' + '<p>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.loading : 'Loading...') + '</p>' + '</div>' + '</div>' + '</div>' + '</div>'; $('body').append(modalHtml); } var modal = $('#gatewaykit-error-details-modal'); var content = $('#gatewaykit-error-details-content'); content.html('<p>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.loading : 'Loading...') + '</p>'); modal.show(); $.ajax({ url: gatewaykit_ajax.ajax_url, type: 'POST', data: { action: 'gatewaykit_get_error_details', transaction_id: transactionId, nonce: gatewaykit_ajax.nonce }, success: function(response) { if (response.success) { var data = response.data; var html = '<div class="gatewaykit-error-details">'; html += '<h4>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.error_summary : 'Error Summary') + '</h4>'; html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.error_type : 'Type') + ':</strong> ' + self.escapeHtml(data.error_type_label || data.error_type || 'Unknown') + '</p>'; html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.error_message : 'Message') + ':</strong> ' + self.escapeHtml(data.error_message || 'No message available') + '</p>'; if (data.error_code) { html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.error_code : 'Code') + ':</strong> ' + self.escapeHtml(data.error_code) + '</p>'; } if (data.error_timestamp) { html += '<p><strong>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.timestamp : 'Timestamp') + ':</strong> ' + self.escapeHtml(data.error_timestamp) + '</p>'; } if (data.error_details && Object.keys(data.error_details).length > 0) { html += '<h4>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.error_details : 'Error Details') + '</h4>'; $.each(data.error_details, function(key, val) { if (typeof val === 'object') { val = JSON.stringify(val, null, 2); } html += '<p><strong>' + self.escapeHtml(key) + ':</strong> ' + self.escapeHtml(val) + '</p>'; }); } var textToCopy = 'Error Type: ' + (data.error_type_label || data.error_type || 'Unknown') + '\n' + 'Error Message: ' + (data.error_message || 'No message available') + '\n'; if (data.error_code) { textToCopy += 'Error Code: ' + data.error_code + '\n'; } if (data.error_timestamp) { textToCopy += 'Timestamp: ' + data.error_timestamp + '\n'; } if (data.error_details) { textToCopy += 'Details: ' + JSON.stringify(data.error_details, null, 2) + '\n'; } html += '<button class="gatewaykit-copy-button" data-copy-text="' + encodeURIComponent(textToCopy) + '">' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.copy_error_details : 'Copy Error Details') + '</button>'; html += '</div>'; content.html(html); } else { content.html('<p>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.error : 'Error') + ': ' + self.escapeHtml(response.data || (gatewaykit_admin_vars ? gatewaykit_admin_vars.unknown_error : 'Unknown error')) + '</p>'); } }, error: function() { content.html('<p>' + self.escapeHtml(gatewaykit_admin_vars ? gatewaykit_admin_vars.ajax_error_occurred : 'AJAX error occurred') + '</p>'); } }); } }; // Expose to GatewayKit namespace and initialize $.extend(window.GatewayKit.Admin, AdminTransactions); $(document).ready(function() { AdminTransactions.init(); }); })(jQuery);